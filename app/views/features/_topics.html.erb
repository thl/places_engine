<% unless category_features.empty? %>
  <ul id="CategoryFeaturesList">
    <%= category_feature_list(category_features).html_safe %>
  </ul>
  <%= note_popup_link_list_for(feature, {:association_type => CategoryFeature.name}) %>
<% end %>
