<% @features_with_parents.collect do |f|
     views = @view.instance_of?(Array) ? @view : [@view]
     names = @view.collect{ |v| f[0].prioritized_name(v).name }.uniq %>
<%=  names.join('; ') %> (<%
     if !f[1].nil?
       names = @view.collect{ |v| f[1].prioritized_name(v).name }.uniq %><%= names.join('; ') %> - <%
     end %><%= f[0].feature_object_types.first.category.header %>) {<%= f[0].fid %>}
<% end %>