<%= error_messages_for model_name %>

<%=

	category_fields({
		:subject => {:display => f_label(@object.feature), :label => f.label(:feature)},
		:root => @parent_object_type,
		:varname => :feature_object_type,
		:include_js => false,
		:selectable => false
	})

%>
