<%
if parent_type.to_s =~ /feature/
	add_breadcrumb_item features_link
end
case parent_type
when :feature
	add_breadcrumb_item feature_link(@object.notable)
end
add_breadcrumb_item link_to(AssociationNote.model_name.human(:count => :many).s, collection_path)
# AssociationNote.search() needs to be modified to allow for an :association_type filter for this to be meaningful.
#add_breadcrumb_item link_to(@object.association_type_name, polymorphic_path([:admin, @parent_object, :association_notes], :association_type => @object.association_type))
%>