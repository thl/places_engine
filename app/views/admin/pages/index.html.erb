<% add_breadcrumb_item link_to('citation', polymorphic_path([:admin, @parent_object.citable, @parent_object])) 
   add_breadcrumb_item 'pages' %>
<div>
	<div class="right highlight">
	<% if parent? %>
		<%= new_item_link [@parent_object, :page] %>
	<% else %>
		<%= parent_resource_dependency_message %>
	<% end %>		
	</div>
</div>
<br class="clear"/>
<%= render :partial => 'pages_list', :locals => { :list => @collection } %>