<%  add_breadcrumb_item link_to(Citation.model_name.human.s, polymorphic_path([:admin, @parent_object.citable, @parent_object]))
    add_breadcrumb_item Page.model_name.human(:count => :many).s
    add_breadcrumb_item ts('new.this') %>
<%= form_for [:admin, @parent_object, @object] do |f| %>
<%=   f.hidden_field :citation_id %>
<%=   render :partial=>'form_fields', :locals=>{:f=>f,:object=>@object} %>
<%=   link_to ts('cancel.this'), collection_path %> | <%= globalized_submit_tag 'creat.e.this' %>
<%  end %>