<%= "#{link_to(ts('edit.record', :what => User.human_name), edit_admin_person_user_path(@person))} |" if !@person.user.nil? %>
<%= link_to ts('delete.this'), admin_person_path(@person), :confirm => te('delete.confirmation'), :method => :delete %> |
<%= link_to ts('back.to', :what => t('all.records', :what => Person.human_name(:count => :many))), admin_people_path %>