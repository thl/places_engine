<% add_breadcrumb_item collection_name %>

<div>
	<div class="left">
		<%= resource_search_form %>
	</div>
	<%= highlighted_new_item_link [:info_source], nil, :align => 'right' %>
</div>

<br class="clear"/>

<% if @collection.empty? %>
	<%= empty_collection_message %>
<% else %>

<table class="listGrid">
	
	<%= pagination_row :colspan=>5 %>
	
	<tr>
		<th class="listActionCol"></th>
		<th>Code</th>
		<th>Title</th>
		<th>Agent</th>
		<th>Date Published</th>
	</tr>
	
	<% @collection.each do |item| %>
	<tr>
		
		<td class="listActionsCol"><%= list_actions_for_item(item) %></td>
		
		<td><%= item.code %></td>
		
		<td><%= item.title %></td>

		<td><%= item.agent %></td>

		<td><%= item.date_published %></td>
		
	</tr>
	<% end %>
</table>

<% end %>