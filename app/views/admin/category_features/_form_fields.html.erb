<%= error_messages_for model_name %>

<%= 
	category_fields({
		:subject_label => (f.label :feature),
		:subject => f_label(@object.feature),
		:root => nil,
		:varname => :category_feature,
		:hastree => 'true',
		:labels => (f.label :text_label),
		:labeln => (f.label :numeric_label)
	}, f )
%>