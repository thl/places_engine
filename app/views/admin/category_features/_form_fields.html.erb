<%= error_messages_for model_name %>
<fieldset>
	<legend>
		Knowledge Map Category
	</legend>
	<div class="row">
		<%= f.label :feature %>
		<span><%= f_label(@object.feature) %></span>
	</div>
	<div class="row">
		<%= f.label :category %>
		<%= category_selector(nil, :category_feature, :category, false, :hasTree => 'true', :singleSelectionTree => 'true') %>
	</div>
</fieldset>