<%  add_breadcrumb_item features_link
    add_breadcrumb_item feature_link(@object.feature)
    add_breadcrumb_item link_to(Topic.model_name.human(:count => :many).s, collection_path)
    add_breadcrumb_item link_to(@object.id, object_path(@object))
    add_breadcrumb_item ts('edit.this') %>
<%= form_for [:admin, @object] do |f| %>
<%=   category_form_table(:form_partial => 'form_fields', :footer => "#{link_to ts('cancel.this'), object_path} | #{globalized_submit_tag 'update.this'}",
        :locals => { :f => f, :object => @object, :single_selection => true }) %>
<%  end %>