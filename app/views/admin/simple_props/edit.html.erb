<%
add_breadcrumb_item link_to(collection_name, collection_path)
add_breadcrumb_item link_to(@object.code, object_path(@object))
add_breadcrumb_item 'edit'
%>

<% form_for [:admin, @object] do |f| %>
	<%= render :partial=>'admin/simple_props/form_fields', :locals=>{:f=>f,:object=>@object} %>
	<%= link_to 'Cancel', collection_path %> | <%= submit_tag 'Update' %>
<% end %>